{
  "name": "crdt-wrapper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "preinstall": "npx only-allow pnpm",
    "prepare": "pnpm run build"
  },
  "workspaces": [
    "wrappers/*"
  ],
  "devDependencies": {
    "esbuild": "^0.23.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}